# ngrok 2.x 配置文件
version: "2"
authtoken: # 请在这里填入你的ngrok authtoken
tunnels:
  frontend:
    addr: 5173
    proto: http
    inspect: true
    # WebSocket支持
    schemes:
      - https
  backend:
    addr: 8000
    proto: http
    inspect: false
    # WebSocket支持
    schemes:
      - https
